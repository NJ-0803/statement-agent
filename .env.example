ANTHROPIC_API_KEY=sk-ant-...
# Optional: Groq categorizes merchants nothing else recognises (free key at console.groq.com).
# Only cleaned merchant words are sent — never amounts, dates, account or card numbers.
GROQ_API_KEY=
# GROQ_MODEL=llama-3.3-70b-versatile
